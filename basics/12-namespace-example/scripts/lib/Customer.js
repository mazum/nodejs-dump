// Customer.js
(function (ns) {

    //Just define the code and
    // add it to the namespace
    ns.Customer = function (name) {
        this.name = name
    };

}(window.WilderMinds = window.WilderMinds || {}));